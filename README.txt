INSTRUCTIONS:


1. Give executable permissions to the programs:
    chmod +x break.pl build_indexes.sh data_retrieval.py prepare_files.py

2. Run prepare_files.py with the appropriately formatted data file as standard
input

3. Run build_indexes.

4. Run data_retrieval to retrieve data using the queries. For more details,
consult the User Guide
